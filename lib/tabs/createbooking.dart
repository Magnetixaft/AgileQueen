import 'package:flutter/material.dart';

class CreateBookingView extends StatelessWidget {
  const CreateBookingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return const Center(
      child: Text("Hello World! Here we should create bookings."),
    );
  }
}
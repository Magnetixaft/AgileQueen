import 'package:flutter/material.dart';

class ChangeBookingView extends StatelessWidget {
  const ChangeBookingView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return const Center(
      child: Text("Please enter your username first"),
    );
  }
}